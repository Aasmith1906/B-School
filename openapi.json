{ "openapi": "3.1.0", "info": { "title": "MBA Rankings API", "description": "Provides historical MBA rankings and school details from 2008 to 2023.", "version": "1.0.0" }, "servers": [ { "url": "https://mba-rankings.example.com/api", "description": "Main API server" } ], "paths": { "/rankings": { "get": { "operationId": "getAllRankings", "summary": "Get all MBA rankings", "description": "Retrieve the full list of MBA rankings for all years.", "responses": { "200": { "description": "List of all rankings", "content": { "application/json": { "schema": { "type": "array", "items": { "$ref": "#/components/schemas/Ranking" } } } } } } } }, "/rankings/{year}": { "get": { "operationId": "getRankingsByYear", "summary": "Get MBA rankings by year", "description": "Retrieve MBA rankings for a specific year.", "parameters": [ { "name": "year", "in": "path", "required": true, "schema": { "type": "integer" }, "description": "The year for which rankings are requested" } ], "responses": { "200": { "description": "Rankings for the specified year", "content": { "application/json": { "schema": { "type": "array", "items": { "$ref": "#/components/schemas/Ranking" } } } } }, "404": { "description": "Rankings for the year not found" } } } }, "/rankings/{year}/{rank}": { "get": { "operationId": "getSchoolByRankAndYear", "summary": "Get MBA school details by year and rank", "description": "Retrieve details for a specific school based on its rank and year.", "parameters": [ { "name": "year", "in": "path", "required": true, "schema": { "type": "integer" }, "description": "The year of the ranking" }, { "name": "rank", "in": "path", "required": true, "schema": { "type": "integer" }, "description": "The rank of the school in that year" } ], "responses": { "200": { "description": "Details of the school for the specified year and rank", "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Ranking" } } } }, "404": { "description": "School not found for the specified year and rank" } } } } }, "components": { "schemas": { "Ranking": { "type": "object", "properties": { "Year": { "type": "integer", "description": "Year of the ranking" }, "Rank": { "type": "integer", "description": "Rank of the school" }, "SchoolName": { "type": "string", "description": "Name of the school" }, "Location": { "type": "string", "description": "Location of the school" } } } } } }